<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.isekai.userservice.mybatisMapper.UserMapper">
    <select id="getUserById" parameterType="int" resultType="com.isekai.userservice.pojo.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <select id="now" resultType="java.time.LocalDateTime">
        SELECT NOW();
    </select>
</mapper>
