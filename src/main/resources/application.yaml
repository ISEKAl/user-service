spring:
  profiles:
    active: dev



---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/user-service
    username: user-service-admin
    password: 114514
server:
  port: 8080

